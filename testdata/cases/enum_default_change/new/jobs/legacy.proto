syntax = "proto2";
package acme.legacy;

enum LegacyState {
  LEGACY_NEW = 0;
  LEGACY_DONE = 1;
  LEGACY_ARCHIVED = 2;
}

message Task {
  optional string id = 1;
  optional LegacyState state = 2;
}
